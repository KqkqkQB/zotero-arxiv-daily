zotero:
  user_id: ${oc.env:ZOTERO_ID}
  api_key: ${oc.env:ZOTERO_KEY}
  include_path: null # Or set a list of glob patterns, e.g. ["2026/survey/**","2026/reading-group/**"]

email:
  sender: ${oc.env:SENDER}
  receiver: ${oc.env:RECEIVER}
  smtp_server: smtp.qq.com
  smtp_port: 465
  sender_password: ${oc.env:SENDER_PASSWORD}

llm:
  api:
    key: ${oc.env:OPENAI_API_KEY}
    base_url: ${oc.env:OPENAI_API_BASE}
  generation_kwargs:
    model: gpt-4o-mini

source:
  semantic_scholar:
    # Broad query for medical image analysis, PET/MRI, and semi-supervised segmentation.
    query: >
      ("medical image segmentation" OR "medical image analysis" OR "PET/MRI" OR "PET MRI" OR "positron emission tomography magnetic resonance" OR "semi-supervised segmentation" OR "semi-supervised medical image segmentation" OR "multimodal medical image segmentation")
    # Recent papers. Use 2025-2026 by default.
    year_from: 2025
    year_to: 2026

    # Keep this modest to avoid API rate limits.
    limit: 50
    api_key: ${oc.env:SEMANTIC_SCHOLAR_API_KEY,null}

    # Keep journal and conference papers.
    publication_types:
      - JournalArticle
      - Conference

    # Abstracts improve embedding-based reranking.
    require_abstract: true

    # Require a journal/conference venue.
    require_venue: true

    # Leave unrestricted first to avoid filtering out useful papers.
    venue_filter: null

  arxiv:
    category: ["cs.CV", "cs.AI", "cs.LG"]
    include_cross_list: true

  biorxiv:
    category: null

  medrxiv:
    category: null

executor:
  debug: ${oc.env:DEBUG,null}
  source: ["semantic_scholar"]
